-- ============================================================================
--  V16 : 卓のエリア（カウンター・小上がり など）
-- ============================================================================
--  店主と合意済み（卓 2/3）。実際の配置は店主待ちだが、列と画面は先に作る。
--  ホール盤面はこの値ごとに見出しで区切る。
--
--  ★ enum（決め打ち）にしない
--    エリア名は店の言葉で店主が決めるもの。コードが先に語彙を決めると、
--    それに合わせて店が呼び方を変えることになる。自由入力の 20 文字。
--
--  ★ NULL を許す（DEFAULT を置かない）
--    未設定は「まだ決めていない」。盤面では「その他」にまとめ、
--    全卓が未設定なら見出し自体を出さない（導入前と見た目が変わらない）。
--
--  ※ 指示文では V15 だったが、会計済み（片付け待ち・卓 1/3）が
--    先に V15 を使ったため V16。
-- ----------------------------------------------------------------------------
ALTER TABLE dining_table ADD COLUMN area VARCHAR(20);
