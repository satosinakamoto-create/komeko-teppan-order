# CLAUDE.md — 米粉と鉄板 モバイルオーダー

このプロジェクトの恒久的な約束事。コードを書く前に必ず目を通すこと。

## プロジェクト概要

学芸大学の鉄板料理居酒屋「米粉と鉄板」向けのモバイルオーダー。
**卓の QR → スマホ注文 → 1 卓 1 伝票に積み上げ → 退店時にまとめて会計。**
**オンライン決済は扱わない**（会計はレジで現金・キャッシュレス端末）。

- 言語: Java 21 / Spring Boot 3.3.5 / Thymeleaf / Spring Data JPA / Spring Security
- ビルド: Maven（`.\tools\run.ps1` で起動。Maven 未インストールでも動く）
- DB: dev = H2 ファイル（`data\komeko.mv.db`）、prod = PostgreSQL
- パッケージ: `jp.komeko.order`

## コマンド

```powershell
.\tools\run.ps1                 # 開発起動（http://localhost:8080）
.\tools\run.ps1 -Test           # テスト
.\tools\run.ps1 -Update         # GitHub の更新を取り込み（テスト込み・要 setup-github 済み）
.\tools\run.ps1 -Package        # 実行可能 jar
.\tools\run.ps1 -Port 8081      # ポート変更
.\tools\setup-github.ps1        # GitHub 連携の初期設定（一度だけ。要 gh auth login）
.\tools\sync-front.ps1          # CSS・JS・テンプレートを反映（アプリは止めない）
```

DB をリセットしたいときは `data\` フォルダを削除して再起動する。

### 画面を直したときは、アプリを止めない（2026-09-12 に確立）

**CSS・JS・テンプレートを直したら `.\tools\sync-front.ps1` を実行する。再起動しない。**

アプリが配信しているのは `src\` ではなく **`target\classes\` にある写し**。
`spring-boot:run` は起動時にコピーを作るだけなので、`src\` を直しても
走っているアプリには届かない。写せば届く（実測: 同じ PID のまま約 2 秒で反映）。

- テンプレート … `spring.thymeleaf.cache=false`（dev）なので写した瞬間に効く
- CSS / JS … ファイル名にハッシュが混ざる作り（`application.yml` の
  `spring.web.resources.chain`）。中身が変わればハッシュも変わるので、
  写せば次の読み込みから新しいものが届く。**ブラウザは `Ctrl + Shift + R`**

**なぜ止めてはいけないか。** Tomcat のセッションはメモリにあり、
プロセスを強制終了すると書き出されずに消える。つまり**再起動するたびに
ログインが切れて、見ている人が締め出される**。
2026-09-12 に、この理由で 1 日に 15 回ログアウトさせた。
「確認できない」と言われたら、まずこれを疑うこと。

**Java（`.java`）を直したときだけは再起動が要る。** コンパイルが必要なため。
このときもログインは切れるので、直す前にひとこと伝える。

### PowerShell スクリプトを編集するときの注意（ハマりどころ）

- **`.ps1` は必ず UTF-8 **BOM 付き** で保存する。**
  Windows PowerShell 5.1 は BOM の無い `.ps1` を ANSI（CP932）として読むため、
  日本語コメントが文字化けし、`。` などが原因で構文エラーになる。
  エディタや自動整形で BOM が落ちたら、こう戻す:

  ```powershell
  $p = ".\tools\run.ps1"
  $c = Get-Content $p -Raw -Encoding UTF8
  [System.IO.File]::WriteAllText($p, $c, (New-Object System.Text.UTF8Encoding($true)))
  ```

- **ネイティブ exe の stderr を `2>&1` で受けない。**
  PowerShell 5.1 は 1 行ずつ ErrorRecord に包むため、
  `$ErrorActionPreference = "Stop"` のもとでは正常終了でも例外になる。
  リダイレクトは `cmd /c "java -version 2>&1"` のように cmd 側にやらせる。

- `&&` / `||` / 三項演算子は 5.1 では使えない。`;` と `if ($?) { }` で書く。

### 日本語を含むデータを、コマンドラインからアプリへ送らない

管理画面のフォームを `curl` で叩いて設定を保存する、といったことをするときは要注意。

**2026-08-18 に店舗設定の日本語 4 項目（店舗名・キャッチコピー・受付停止中の
メッセージ・お客さまへのご案内）を全部 `?????` に破壊した。**
原因はアプリ側ではなく、Git Bash 上の `grep` / `sed` で HTML から既存値を
取り出す段階で、ロケールの都合により多バイト文字が `?` に潰れていたこと。
その `?` をそのまま POST したので、アプリは受け取ったとおりに保存した。

- **画面の操作はブラウザでやる。** これが一番確実。
- どうしてもコマンドラインから送るなら、値をシェル変数に入れず
  **UTF-8 のファイルに書いて `curl --data-urlencode "name@file"` で渡す**。
  バイト列がシェルを通らないので潰れない。
- 送ったあとは**必ず画面で読み直して確認する**。
  この種の破壊は例外も警告も出さず、次に画面を見るまで気づけない。

> 商品名・カテゴリ名など、画面から入力したデータは無事だった。
> 壊れたのはコマンドラインで書き戻した項目だけ。

### Figma から実装するときは、スクショを見ない。実数値で測る（2026-09-19 に確立）

**設計を見て実装しない。設計を測って実装する。**
Figma は MCP でつながっている。値は取り出せる。**目で見て写すのは禁止。**

店主に何度も同じ指摘をさせた。原因はいつも同じで、
**スクリーンショットを見て「だいたい合っている」と判断したこと**。

```
指摘された内容                      目で見た結果   測った結果
品切れの色が違う                    緑っぽい       5 か所ちがう
見出しの位置が微妙にズレる          ほぼ同じ       0 / 6 / 7px の 3 種類
モーダルの余白が狭い（1 か所の指摘） 1 か所         4 か所ちがう（原因は gap 1 つ）
店舗設定が昔のまま                  並べ方は近い   カード 4 つと項目 18 個が余計
```

**gap 20px と 32px の違いは、スクショでは見分けられない。** 測れば一度で出る。

#### 手順

1. **設計側を取り出す。** `use_figma` で対象フレームを walk し、
   寸法・padding・gap・角丸・塗り・枠・文字（サイズ / 太さ / 色）を全部出す。
2. **実装側を測る。** `target/allscreens` に対して probe ページを作り、
   `getComputedStyle` と `getBoundingClientRect` で同じ項目を出す
   （既存の `_preview-*.html` が見本。作り方はそれを真似る）。
3. **差分の一覧を作る。** 「実装 → 設計」の表にして、**直す前に店主へ出す。**
4. 直す。**直したら必ず測り直す。** 測らずに「直りました」と言わない。

#### 踏んだ罠（同じところで転ばないこと）

- **構造の取り出しが空で返ってきたら、そこで止まる。**
  `findOne('本文')` が null を返したのに、スクショで代用して進めて間違えた。
  空が返るのは「構造が違う」という情報。見なかったことにしない。
- **指摘は 1 か所でも、原因は 1 か所とは限らない。**
  モーダルの余白は「段の間 20px（設計 32px）」1 つが全段に効いていた。
  指摘された箇所だけ直すと、同じ指摘をあと 2 回もらう。
- **効いている場所を取り違えない。**
  `.hallmodal__box` の gap を直しても 1 ミリも動かなかった。
  段を積んでいたのは `.hallmodal__pane` のほうだった。**測れば分かる。**
- **`box-sizing: border-box` に注意。**
  行に `min-height: 50px` と書いても padding 32px を含むので、中身の床は 18px。
  床は「行」ではなく「中身」に敷く。

## コーディング規約

### 全般

- **Lombok は使わない。** getter / setter は手書きする（初学者が読める状態を保つため）。
- コメントと画面文言はすべて日本語。
- 学習用プロジェクトなので、**「なぜそう書くか」のコメントを厚めに**書く。
  ただし処理の逐語訳（`// i を 1 増やす`）は書かない。
- クラス名・メソッド名は英語、変数名も英語。日本語識別子は使わない。
- **「カテゴリ」は商品カテゴリ（`Category`／お品書きの 14 分類）だけを指す。**
  2026-09-16 まで、品切れ・残数の「カテゴリーから検索」は**商品**カテゴリ、
  食材・在庫の「カテゴリーから検索」は**食材**の分類で、同じサイドバーの中で
  同じ文言が別のものを指していた。コードでも `categoryGroups` という同じ名前が
  両方で使われていて、**作った本人が読み違えた**（食材の分類はこれもあって廃止）。
  - 商品カテゴリ以外のまとまりを増やすときは、画面の文言もモデル属性名も
    **「カテゴリ」以外の語**にする（「種類」「費目」など）。
  - 費目（`PurchaseCategory`）は名前に Category が入っているが別物。
    こちらは `isFoodCost()` が実際原価率に効く**計算に使う値**なので消さないこと。

### 金額・会計（最重要）

- **金額は必ず `int`（円）**。`double` / `float` は禁止（誤差が出る）。
- 価格は**税込**で保持する（日本の総額表示義務に合わせる）。
- 税額は `TaxCalculator.includedTax(税込金額, 税率)` で逆算する。自前で計算しない。
- 税率 10%（酒類を扱うため軽減税率の対象外）。`ShopSetting.taxRatePercent` から取り、ハードコードしない。
- 注文には**注文時点の税率と価格をスナップショット**として保存する。マスタを参照しない。
- **伝票の金額計算は `TableSession.recalculate` の 1 箇所だけ。**
  他の場所で小計・チャージ・深夜料金を再実装しない。表示は getter を読むだけにする。

  ```
  小計            = キャンセル以外の注文の合計（税込）
  テーブルチャージ = tableChargePerGuest × guestCount
  深夜料金の対象額 = Σ（深夜帯に出した注文の合計）
                   ＋ 深夜帯に着席していれば テーブルチャージ
  深夜料金        = 対象額 × lateNightSurchargePercent / 100   ※切り捨て
  ご請求額        = 小計 + テーブルチャージ + 深夜料金
  ```

- **深夜料金は「注文時刻」で決まる。会計時刻ではない。**
  注文は `Order.createdAt`、テーブルチャージは `TableSession.openedAt` で判定する。
  同じ伝票に通常料金の注文と深夜料金の注文が混在する。
  判定役は `LateNightPolicy`（関数型インタフェース）で渡す。真偽値ひとつでは表現できない。
  切り捨ては**対象額を合計してから 1 回だけ**（注文ごとに切り捨てると、分けて頼むほど安くなる）。
- **深夜料金の免除は 2 段階。どちらも「人の判断」なので DB に記録する。**
  - `Order.lateNightExempt` … その注文だけ対象外。**打ち直しの救済**が主用途。
    22:55 の注文を 23:05 に取り消して 23:06 に入れ直すと時刻が新しくなり 10% が乗る。
    店のミスで高くなるので、明細ごとに外せるようにしてある（何度でも切り替え可）。
  - `TableSession.lateNightWaived` … 伝票全体（チャージ含む）。会計時のサービス対応用。
  どちらも「計算し直したら消えた」が起きてはいけない。
  `LateNightPolicy.NONE` を渡すだけだと reopen 後の再計算で免除が消え、
  お客さまに伝えた金額より高い額に戻る。人の判断は計算結果で上書きしない。
- **注文時刻（`Order.createdAt`）は人が書き換えられるようにしない。**
  提供時間の集計にも使う事実の記録。事実は残し、判断は別項目で持つ。
- **キャンセルした注文は請求から外す。** 注文の状態が変わったら必ず伝票を再計算する
  （`OrderService.refreshSessionOf`）。ここを忘れると「取り消したのに減らない」不具合になる。
- 伝票は**来店時点の設定をコピー**して持つ。設定変更は既存の伝票に影響しない。
- **オプションの追加料金は 0 円以上。** 値引き（マイナス）を許すと単価が負になり、
  その卓の小計から他の品の代金が引かれる。商品価格も 0 以上なので、
  ここを閉じておけば単価は構造的に負にならない。緩めるときは
  「値引き額は商品価格を超えない」という商品単位の検査とセットで。
- **営業中に価格を変えるときは「いったん販売中止 → 価格を変更 → 販売再開」。**
  価格が動く瞬間に誰も注文できない状態を作るための手順。
  この手順を飛ばすと、カートを持っているお客さま全員の注文が一度止まる
  （コードは検知して止める。止まらずに済ませるのは運用の仕事）。詳細は `docs\仕様.md` 4.6.1。
  **深夜帯の時刻・割増率の変更も同じ扱い。** 割増率は伝票が来店時点でコピーするが、
  深夜帯の時刻窓だけは常に現在の設定を読むため、営業中に動かすと
  OPEN の伝票で「どの注文が深夜扱いか」が変わる。
- **注文は「全部か無か」ではない。** 売り切れた品だけ落として残りは通す。
  金額や中身が変わったときだけ止める。`CartService.CartRefresh` の 2 本立てを崩さないこと。

### 卓と伝票

- `DiningTable`（卓）は固定。QR には `/t/{accessToken}` が入る。**連番 ID を URL に出さない。**
- `TableSession`（伝票）は来店ごと。1 卓につき OPEN は 1 つだけ。
- お客さんのブラウザは `TableContext`（`@SessionScope`）で卓を覚える。
  カートを空にしても卓の紐づけは消さない。
- 会計＝`TableSession` を CLOSED にすること。売上はここで確定する。

### JPA / DB

- `open-in-view: false`。**画面描画時には DB 接続が無い。**
  必要な関連は Service の `@Transactional` の中で読み終えてから返すこと。
  遅延読み込みの実体化は `getXxx().size()` を呼ぶ（`hydrate` という名前のメソッドにまとめる）。
- 関連は原則 `FetchType.LAZY`。まとめ読みが必要なら `@EntityGraph` か `@BatchSize`。
- `List` を 2 段まとめて `JOIN FETCH` すると `MultipleBagFetchException` になる。
  1 段だけ fetch し、もう 1 段は `@BatchSize` で解決する。
- テーブル名 `orders`（`ORDER` は SQL の予約語）。
- 本番は `ddl-auto: validate`。スキーマ変更を入れるときは Flyway 導入を検討する（未導入）。

### Web 層

- **PRG パターン厳守。** 更新系の POST は必ず `redirect:` で返す。
- フォームは必ず `th:action="@{/...}"`。素の `action=` は CSRF で 403 になる。
- HTML の `<form>` は入れ子にできない。
- 画面に出すメッセージは `RedirectAttributes` のフラッシュ属性で渡す。
  キーは `flashSuccess` / `flashInfo` / `flashWarn` / `flashErrors`（`List<String>`）に統一。

  **色は 2 つだけ（2026-09-20・店主の決定「警告ポップアップは赤、変更とかは緑で
  良いんじゃない？ポップアップは押せないものでしょ？」）。**

  | キー | 色 | 出し方 | 中身 |
  |---|---|---|---|
  | `flashSuccess` | 緑 | 浮かせて 3 秒 | 変更が成った（追加・更新・削除） |
  | `flashInfo` | 緑 | 浮かせて 3 秒 | 変更が成った（止めた・外した・空にした） |
  | `flashWarn` | 赤 | 浮かせて 3 秒 | 警告・できなかった |
  | `flashErrors` | 赤 | **本文に残す** | なぜ保存できなかったか（箇条書き） |

  - **`flashSuccess` と `flashInfo` は同じ色。** 分かれているのは呼び出し側の
    書き分けが 65 / 19 箇所あるからで、見た目に差はない。新しく書くなら
    `flashSuccess` でよい。
  - **青（`.alert--info`）は浮かせる札には使わない。** 2026-09-20 まで
    `flashInfo` は青だったが、中身の 8 割が「掲載を止めました」など
    緑と同じ種類の報告で、**「掲載しました」は緑、「掲載を止めました」は青**と
    割れていた。色が何も言っていなかった。
  - **赤は `.alert--error` に寄せる。`.alert--warn` を使わない。**
    `.alert--warn` はきつね色の**常設の案内**で既に 38 箇所
    （「カテゴリがありません」「受付を止めています」）。あれは状態であって
    報告ではない。同じ名前に赤を足すと、JS が動かなかったときに
    同じクラスが画面によって赤ときつね色に割れる。
  - **`flashErrors` は浮かせない。** 複数行になることがあり、
    3 秒で消えると「なぜ保存できなかったか」が読めなくなる。
  - 守り: `ToastColorTest`（色は 2 つ・青は作らない・押せる塗りは使わない）。
- エンティティを直接フォームにバインドしない（Form クラスを噛ませる）。
  例外は `ShopSetting`（`ShopSettingService.save` が必要な項目だけ写している）。
- 業務ロジックはコントローラに書かず Service に置く。

### Thymeleaf

- レイアウトは 3 種類。ページ側は必ずこの形で書く。

  ```html
  <html th:replace="~{layout/customer :: layout('タイトル', ~{::main})}">
  <body><main> … </main></body></html>
  ```

  | レイアウト | 用途 |
  |---|---|
  | `layout/customer` | お客さん向け |
  | `layout/staff` | 厨房・管理 |
  | `layout/plain` | ログイン・エラー・印刷 |

- **同じタグに `th:if` と `th:replace` を書かない。** `th:replace` が先に評価され `th:if` が無視される。
  条件は外側のタグに書く。
- プロパティ記法は getter に対応する（`isXxx()` / `getXxx()` → `${obj.xxx}`）。
  record のアクセサは `${obj.xxx()}` と直接呼ぶ。
- 金額表示は `${#numbers.formatInteger(v, 1, 'COMMA')}`。
- **モデル属性に `session` / `param` / `request` / `application` という名前を使わない。**
  Thymeleaf の予約変数と衝突する。伝票は `bill` という名前で渡している。

### CSS

- **テーマは 3 枚の層でできている（2026-09-01 に整理）。**
  下の層ほど広く効く。**どの層に書くかを間違えるのが、この CSS のいちばんの事故**。

  | 層 | クラス | 効く範囲 | 何を決めるか |
  |---|---|---|---|
  | 土台 | `:root` | **全画面**（お客さん含む） | 素材色・余白の尺度・角丸・書体・`--tap` |
  | 配色 | `.theme-snow` | **全画面**（4 レイアウト全部） | 白基調の配色と質感（app.css 21 節） |
  | 寸法 | `.theme-desk` | 机で読む画面 27 枚のみ | 文字寸法・きつね色・表の塗り（app.css 30 節） |
  | 上振れ | `.theme-ledger` | 税理士 6 枚のみ | 補足文の 16px 床・行間（`.theme-desk` と 2 枚重ね） |

  - **`.theme-snow` は「スタッフ側だけ」ではない。** 08-18 夜にお客さん側へ広げたので、
    `customer` / `staff` / `accountant` / `plain` の 4 レイアウト全部に付いている。
    ここに寸法や管理画面向けの調整を書くと、**注文画面に必ず漏れる**。
  - **`.theme-desk` はスタッフ側の全画面に付く。**
    `layout/staff.html` の body が `class="theme-snow theme-desk"` の**固定**なので、
    厨房 `/kitchen`・ホール `/hall` にも付いている（税理士画面も常時）。
    <br>※ ここには長く「厨房・ホールには付けない」と書いてありましたが、**誤りでした**
    （2026-09-07 に実測で判明）。付かない前提で `.theme-desk` に書くと、
    気づかないまま厨房とホールに漏れます。
  - **立って見る画面（厨房・ホール）を小さく組みたいときは、その画面に絞る。**
    ホールは `.hallboard`、食材・在庫は `.inv-ingredients` のように目印を 1 つ付け、
    `.theme-desk .hallboard …` の形で上書きする。共通の値は動かさない。
    行を大きくすると 1 画面に入る注文数が減るほうが痛い、という判断は変わっていません。
  - `:root` の寸法トークン（`--sp-*` `--r-*` `--tap`）は `.theme-snow` でも
    上書きしていない完全な共有物。ここを動かすと全画面が動く。
  - お客さん側の骨格は和モダン（生成り／きつね色／明朝）のまま。`:root` が正。
- スタッフ側のナビは `layout/staff.html` のサイドバーに一本化。
  現在地は URL（`${currentPath}`）から判定するので、**コントローラに目印を渡す必要はない**。
  スタッフ用の URL を増やしたら、サイドバーの `startsWith` 判定にも追加すること
- アイコンは `fragments/icons.html`（Phosphor duotone / MIT）。
  `<img>` ではなくインライン SVG＋`fill="currentColor"` で文字色に追従させる。
  増やすときは同じ Phosphor duotone から（`tools` の生成手順はコミット履歴参照）
- **`src/main/resources/static/css/app.css` が唯一の正。** 新しいクラスを勝手に増やさない。
  既存クラスの組み合わせで足りないときだけ追加し、追加したらこの規約の意図（下記）に沿わせる。
- デザインコンセプトは「鉄板の熱と、米粉の白」。
  生成り（面）／鉄黒（線・夜）／きつね色（アクセント）の 3 色構成。
  **アクセント色は「押せるもの」「熱いもの」だけに使う。**
- 見出しは明朝（`--font-serif`）、本文はゴシック（`--font-sans`）。
- 外部フォント・外部 CDN は読み込まない（店内 Wi-Fi にネットが無くても崩れないようにする）。
- タップ領域は 48px 以上（`--tap`）。入力欄の `font-size` は 16px 未満にしない（iOS が勝手に拡大する）。
- ダークモードは `prefers-color-scheme` で自動追従。新しい色はセマンティック変数経由で使う。

### 地を決め打ちにしたら、文字も決め打ちにする（2026-09-22 に事故った）

**`var()` の文字色は `var()` の地とだけ組ませること。**

浮かせる札（toast）で、地を `background: #eef7f2` と決め打ちにしたのに、
文字色は `.alert--success` から降ってくる `var(--text)` のままにした。
**お客さまの注文画面は `.theme-night`（暗い地）で `--text: #ffffff`。**
結果、薄緑の上に白文字が出て**コントラスト 1.09:1**——「注文リストに追加しました」が読めなかった。

赤の札も同じ穴で、`var(--danger)` は暗いテーマ #e28a80 で 2.23:1、
**明るいテーマの #d33f3f でも 4.01:1** と、どちらのテーマでも AA(4.5:1) に届いていなかった。

- **決め打ちの地には決め打ちの文字。** 浮かせる札はどのテーマの上にも同じ顔で出るので、
  テーマ変数に依存させてはいけない。
- 色を足したら**必ずコントラストを計算する**。目で見て「読める気がする」は判断にならない。
  `ToastColorTest` が計算して 4.5:1 を要求している。
- **お客さま側は `.theme-snow` ではなく `.theme-night`。** 4 レイアウトのうち
  `customer` だけが暗い地（`layout/customer.html` の body）。
  スタッフ側で確かめただけで「直った」と言わないこと。

### 表の列幅は px で書かない（`table-layout: fixed` の罠）

**px の列幅が合計 1120 あると、`width: 100%` と書いても表は 1120 から縮まない。**

本文は 1432 幅で 1120px、iPad（1024）では 897px しかない。
px 指定のままだと iPad で右の列が初期表示から見切れる（2026-09-22 に 6 種の表で実測）。

- 列幅は **1120 基準の割合（%）** で書く。比は設計のまま変わらない。
- 併せて **`1fr` ではなく `minmax(0, 1fr)`** と書くこと。`1fr` の下限は「中身の最小幅」なので、
  縮まない塊（QR の画像 220px・決まった幅のボタン）を抱えた列はそこで止まり、
  はみ出したぶんだけ画面ごと横に動く。
- 固定幅のボタンが列に入らないときは `@media (max-width: 1100px)` で幅の主張をやめる。
  **区切りは 1100px。** 既にこのファイルで使っているタブレットの境目に合わせる
  （新しい境目を増やすと、どの幅で何が変わるのか追えなくなる）。

### 画面ダンプ（`AllScreensDumpTest`）は黙って嘘をつく

2026-09-22 の監査で、3 画面が**中身が無い／別の画面**だと分かった。
**どれも例外も警告も出ず、ファイルはできている。** 開くまで誰も気づけない。

| 症状 | 原因 |
|---|---|
| `c01-table-entry` が 0 バイト | 卓に伝票が開いていて `/t/{token}` が **redirect** を返していた |
| `c03b-order-placed` がカート画面 | **営業時間 17:30〜翌2:00 の外で撮ると注文が弾かれる** |
| 消したはずの画面が 0 バイトで残る | 撮る前に古いファイルを消していなかった |

- お客さま側を撮る卓は **「使える・片付け済み・伝票なし」の 3 条件**で選ぶ。
  「伝票が無い」だけでは足りない（会計後は `needsCleanup` で `/start` が弾かれる）。
- 時刻に依存する画面は、撮影中だけ `ShopSetting.alwaysOpen` を立てる。
- **行き先を確かめてから撮る。** redirect 先が想定と違えば例外を投げて止める。
  撮れなかったことが、撮った本人に分かるようにしておく。

### 実装を実測するときの道具（`target/allscreens` のプローブ）

- **`chrome --headless` に `--allow-file-access-from-files` を付ける。**
  無いと iframe の `contentDocument` が null になり
  「Cannot read properties of null」で測れない。
- **iframe は 1 枚ずつ。** 10 枚同時に読むと `--virtual-time-budget` の中で
  `onload` が揃わず `MEASURING` のまま終わる。`_measure.html` と同じ「1 画面 1 起動」にする。
- 結果は **`<pre id="out">` の中身から**取り出す。`JSON_START` を後ろから探すと
  **スクリプト本文**（`'JSON_START' + JSON.stringify(...)` という行）に当たる。
- プローブのファイル名は **`_` で始める**。ダンプ生成が撮る画面だけを消すので、道具は残る。

### セキュリティ

- 認証・認可は Spring Security に任せる。自作しない。
- パスワードは BCrypt。平文で保存・ログ出力しない。
- お客さんの注文は連番 ID ではなく `publicToken`（UUID）で参照する。
- 価格・品切れ・選択肢の妥当性は**必ずサーバ側で再検証**する。
  セッションのカートの値をそのまま信じて会計しない（`CartService.refresh`）。
- アップロードファイル名は必ず自前で作り直す。元のファイル名を使わない。
- 認可ルールを増やすときは `SecurityConfig` の URL とコントローラのマッピングを必ず突き合わせる。
- スタッフ用 URL は `app.staff-access.allowed-ips` で接続元 IP を制限できる
  （`StaffZoneIpFilter`）。スタッフゾーンに URL を足したら、このフィルタの
  `STAFF_PREFIXES` にも追加すること。localhost は常に許可（閉め出し防止）。

### テスト

- 単体テストは Spring を起動しない素の JUnit で書く（速さが正義）。
- DB が要るものだけ `@SpringBootTest` + `@ActiveProfiles("test")`。
- `@DisplayName` は日本語。**そのテストが何を守っているかをコメントで書く。**
- 金額計算・状態遷移・カートのマージは必ずテストを書く（壊れると実害が出るため）。

## ドキュメントの置き場所

- 仕様・設計判断 → `docs\仕様.md`
- 現在地・次にやること → `docs\引き継ぎ.md`
- Java / Spring の学習メモ → `docs\Java学習ガイド.md`
- 恒久的な約束事 → この `CLAUDE.md`

## やらないと決めたこと

意図的に入れていない。増やす前にここを読むこと。

- **食材の分類**（野菜・肉・魚介…／2026-09-16 に削除。V14 で足し、V17 で落とした）
  店主の「分類いるこれ？」から全参照をあたったところ、**計算にいっさい使われて
  いなかった**（税率・在庫・消費・原価・集計すべて 0 件）。分類を 1 件も付けなくても
  原価率も在庫も税額も同じ数字が出るのに、一覧は「分類が決まっていない食材が
  N 件あります → まとめて分類する」と催促していた。**何も産まない作業の催促**だった。
  - **本当に効いているのは入り数の学習（`ItemAlias`）のほう。**
    「エリンギ 120」を一度だけ「食材エリンギの 100g」と教えると次から自動で在庫に積まれる。
    こちらは教えないと数字が変わる。**探すための札と、計算に効く紐付けを混同しないこと。**
  - 作り直すなら、レシート読取が既に Claude を呼んでいるので、
    その JSON スキーマに 1 項目足して**自動で埋める**ほうから検討する。
    人に貼らせる画面をもう一度作らない。
- オンライン決済（会計はレジ）
- テイクアウト・番号呼び出し・サイネージ
  （2026-08-16 にイートインへ方針転換して削除。コードは git 履歴の `376875e` に残っている）
- 会員登録・ポイント
- 複数店舗対応
- 曜日別の営業時間（土日 16:00 開店に未対応。設定は 1 組だけ）
- 複数サーバでの冗長構成（SSE がインメモリのため 1 台前提）
- 年齢確認（スマホからはできない。提供時に店頭で確認する運用）
- **ログイン試行制限・ロックアウト**（2026-08-17 決定）
  店内 LAN 前提では総当たりの現実性が低く（BCrypt＋ランダム初期パスワード）、
  営業ピーク中に厨房端末がロックアウトされる実害のほうが大きいと判断。
  **インターネットに公開する場合は必須になる**ので、そのときに再検討する。
- **DB・バックアップの暗号化**（2026-08-17 決定）
  個人情報を収集しない設計のため、盗難時の被害上限が売上数字どまり。
  逆に鍵管理を失敗すると自分のバックアップが開けなくなるリスクのほうが大きい。
  予約名簿など個人情報を扱い始めたら再検討する。
- **HTTPS**（店内 LAN 運用の間は導入しない）
  自己署名証明書はお客さまのスマホに警告が出て注文の UX を壊す。
  代わりにネットワーク分離で守る（docs/ネットワーク設定.md）。公開時は必須。
