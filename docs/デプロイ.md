# 公開デモのデプロイ

ポートフォリオを見に来た方に**実際に触ってもらう**ための公開手順。
実店舗の運用（prod）とは目的も設定も別なので、`demo` プロファイルを用意してある。

| | 目的 | DB | データが消えたら |
|---|---|---|---|
| `dev` | 手元で作りながら動かす | H2 ファイル | 困る（バックアップあり） |
| `prod` | 実店舗 | PostgreSQL | **絶対に困る** |
| `demo` | 誰でも触れる公開版 | H2 ファイル | **むしろ好都合**（毎回作り直す） |

---

## 1. 設定するのは環境変数 2 つだけ

ホスティングの管理画面で設定する。

| 環境変数 | 値 | いつ入れるか |
|---|---|---|
| `SPRING_PROFILES_ACTIVE` | `demo` | **作成時**（必須） |
| `APP_BASE_URL` | `https://＜公開URL＞` | 作成後でよい |
| `INITIAL_ADMIN_PASSWORD` | デモ専用の使い捨て | 入れておくと楽（下記） |

### いま動いているもの

| | |
|---|---|
| URL | https://komeko-teppan-order.onrender.com |
| サービス | Render / Docker / Free / Singapore |
| リポジトリ | `satosinakamoto-create/komeko-teppan-order`（main への push で自動デプロイ） |

### `INITIAL_ADMIN_PASSWORD` を入れておく理由

`DataSeeder` は管理者がいなければ作り、**パスワード未指定ならランダムに生成して
ログに 1 回だけ出す**。手元ではこれで困らない。`data\` の DB が残るので、
最初に決まったパスワードをずっと使えるからだ。

Render の無料枠は違う。15 分の無通信でスリープし、次の起動は**まっさらなコンテナ**、
つまり空の DB になる。起動のたびに `DataSeeder` が走り、
**パスワードが毎回作り直される**。

つまり「公開前に管理者パスワードを変更する」が原理的に成立しない。
変更しても次のスリープ復帰で消える。ADMIN で入りたければ、
そのたびに Render のログから生成値を拾うことになる。

環境変数に入れておけば、起動のたびに同じ値になる。
**値はコードにも yml にも書かず、Render の Environment 画面にだけ置くこと。**

> ゲストは代わりにならない。`GuestLoginController` は `StaffRole.STAFF` 固定で、
> `/admin/**` の設定変更系には入れない（それが狙いなので、ここは変えない）。

### `APP_BASE_URL` は後回しでいい

公開 URL はサービスを作った**あとに**発行されるので、
最初の 1 回目は入れようがない。

そのため `application-demo.yml` の既定値を `${APP_BASE_URL:}`（空）にしてある。
未設定でも起動し、QR の飛び先が `http://localhost:8080` のあいだは
QR 画面の上部に「この値のままでは使えません」と警告が出る。

> はじめは `${APP_BASE_URL}` と書いていた（既定値なし）。
> これだと変数が無いとき Spring が
> `Could not resolve placeholder 'APP_BASE_URL'` で**起動そのものを中止**する。
> 表示用の URL が分からないというだけでシステム全体が立ち上がらないのは、
> 釣り合いが取れていない。**止めるのではなく、気付ける形にして動かす。**

### `APP_BASE_URL` がすべてを決める

**この値が QR コードに焼き込まれる。**

```
QR の中身 = ${APP_BASE_URL}/t/{その卓のトークン}
```

手元の LAN アドレス（`http://192.168.1.4:8080`）のままだと、
QR を読んでも外からは開けない。QR ページの「この卓のお客さま画面を開く」も同じ。

**正しく設定すれば、見に来た方が自分のスマホで QR を読んで注文できる。**
デモとしてはこれがいちばん強い。

> `https://` を付けること。付け忘れると混在コンテンツでブラウザに止められる。

---

## 2. `demo` プロファイルが自動でやること

`application-demo.yml` に書いてある。手で設定する必要はない。

- **ログイン画面に「ゲストで参加する」を出す**（`app.guest-login`）
- **起動時に営業中の状態を作る**（`app.demo-data`）
  厨房ボードが 3 列とも埋まり、在庫の残数・売り切れも映る
- **24 時間受付にする**
  実店舗の営業時間は 17:30〜翌 1:30。そのままだと昼間に見に来た方は何も注文できない
- **H2 のファイル DB を使う**（PostgreSQL を用意しない）
- **バックアップを止める**（消えて困るデータが無い）
- **H2 コンソールを閉じる**（誰でも触れる場所に DB の入口を出さない）

### 再起動でデータが消えるのは、むしろ好都合

無料ホスティングの多くは再起動でファイルが消える。
本来は困った性質だが、デモに限っては

- 誰かが注文を荒らしても、次の再起動できれいに戻る
- DB の契約も期限も要らない

消えて困るデータが最初から無いので、この割り切りが成立する。

---

## 3. Docker で動かす

リポジトリ直下の `Dockerfile` をそのまま使える。Render / Fly.io / Railway など Docker が使えるところならどこでも。

マルチステージにしてあるので、最終イメージに Maven もソースも入らない。
イメージが小さいほど起動が速く、**無料枠のコールドスタートが短くなる**。

メモリは `-XX:MaxRAMPercentage=70` を指定済み。
無料枠は 512MB のことが多く、指定しないと JVM がヒープを取りすぎてコンテナごと止められる。

### 非 root で動かすなら、書き込み先の所有権まで含めて一組

最初の Dockerfile は `USER komeko` に切り替えた**あとで** `mkdir -p /app/data` を書いていた。
`WORKDIR /app` は root が作る（所有者 root・755）ので、
`mkdir` が Permission denied で落ち、**docker build がそこで止まった**。

仮にビルドが通っても起動できない。`/app/data` が root 所有のままだと

- H2 が `./data/komeko-demo` を作れない
- `ImageStorageService` はコンストラクタで `Files.createDirectories` を呼び、
  失敗すると例外を投げる（Bean が作れず、アプリごと起動失敗）

`mkdir` と `chown -R komeko:komeko /app` を `USER` 切り替えの前に置いて解決。
**権限を下げるだけでは足りず、下げた先に必要な権限を渡すところまでが一組。**

### タイムゾーンはコードで固定する（環境変数に頼らない）

コンテナの既定は UTC。このアプリは営業日の切り替え（5:00）・ラストオーダー・
深夜料金をすべて**日本時間の壁時計**で判断するので、UTC のままだと 9 時間ずれる。

厄介なのは**例外が出ない**こと。厨房ボードは「その営業日ぶん」しか表示しないので、
**起動時に入れたデモの注文が 1 件も出ない**という形でだけ現れる。

> **踏んだ事故（2026-08-22）**: Dockerfile に `ENV TZ=Asia/Tokyo` と
> `-Duser.timezone=Asia/Tokyo` を二重に指定してあったのに、
> 本番（Render）では**どちらも効いておらず UTC で動いていた**。
> JST 13 時の伝票に深夜料金 10% が乗り、売上の営業日も 1 日ずれていた
> （UTC 23:00〜05:00 = **JST 8:00〜14:00** なので、日本の日中に毎日出る）。
> ホスティング側の環境変数や起動コマンドは画面の操作ひとつで消え得る。
>
> このため `KomekoOrderApplication` の static イニシャライザで
> `TimeZone.setDefault(Asia/Tokyo)` を**コードで**固定した。
> Dockerfile の指定は残してあるが、あれは保険であって本体ではない。
> Render 側の環境変数がなぜ効いていないかは未解明（ダッシュボードの
> Docker Command 上書きが怪しい）。コード固定後はどちらでも正しく動く。

---

## 4. 眠らせない

無料ホスティングは無通信が続くとサーバを眠らせる。
次に開いた人は待たされる。**採用担当は待たない。**

> 待ち時間は当初 30〜40 秒と見積もっていたが、**2026-08 の実測では 158〜168 秒**だった。
> 桁が違うので、叩き役のタイムアウト設定はこの実測値を基準にすること。

`/ping` が `ok` とだけ返す軽い入口なので、外から定期的に叩いて起こしておく。

> **アプリの中にタイマーを置いても無意味。**
> 眠っている間はそのタイマー自体が止まっている。自分で自分は起こせない。

### 何で叩くか

**選定の決め手は「タイムアウトを 168 秒以上にできるか」。** ここを外すと何を選んでも失敗する。
komeko のコールドスタートは実測 158〜168 秒あり、30 秒上限の仕組みでは起動を見届けられない。

**要件は 2 つあり、外部サービスで単独に両方を満たすものは無かった。**

| | ①10 分おきに確実に発火 | ②Render を起こせる（＋168 秒待てる） |
|---|---|---|
| **このマシンのタスクスケジューラ** | ✅ | ✅ 住宅 IP なので起こせる（実証済み） |
| cron-job.org | ✅ 正確（履歴 26 回/日で確認） | ❌ `hibernate-wake-error` で拒否。上限 30 秒 |
| GitHub Actions の `schedule` | ❌ **1 日 3 回しか走らない**（実績） | ✅ 実測 157〜168 秒で 200 |
| GitHub Actions の `workflow_dispatch` | ー（外から叩く必要あり） | ✅ 同上。**即座に拾われる** |
| UptimeRobot | ✅ | ❌ タイムアウト上限 30〜60 秒で足りない |

**採用: このマシンのタスクスケジューラ（本命）＋ GitHub Actions の `schedule`（保険）。**
外部サービス同士を繋ぐ案（cron-job.org →`workflow_dispatch`）も部品まで用意してあるが、
GitHub トークンの発行と保管が要るので保留にしてある（下記「叩き役の構成」）。

**採用した組み合わせ: ① を cron-job.org、② を GitHub Actions。**（下記「叩き役の構成」）
自宅 PC は単独で両方満たすが、PC が起動している時しか効かないので本命にはしていない。

### 24 時間叩いてはいけない理由は、もう一つある

`DemoDataSeeder` は**起動時に 1 回だけ**走る。
一方、厨房ボードは `OrderService.kitchenBoard()` が
`currentBusinessDate()` で**その営業日ぶん**に絞り込む。

つまりインスタンスが 5:00（営業日の切り替え）を跨いで生き続けると、
起動時に入れたデモの伝票は前営業日のものになり、**ボードが 3 列とも空になる**。
再シードは走らない（開いている伝票があると `seed()` は何もせずに帰る）。

夜のあいだ眠らせておけば、朝の最初のアクセスで起動し直され、
その日の営業日でデモデータが入り直る。
**「8〜17 時だけ叩く」は節約のためだけの設定ではなく、ボードを埋めておくための設定でもある。**

**時間帯を絞るのが要点。** Render の無料インスタンス時間はアカウント全体で月 750 時間程度。
24 時間起こすと 1 サービスで月 730 時間になり、2 つ動かした時点で破綻する。
8〜17 時なら 1 サービス月 300 時間で、2 つでも収まる。

### 叩き役の構成（2026-09-06〜）

**本命はこの PC のタスクスケジューラ。** 資格情報を一切必要とせず、
要件を単独で両方満たす唯一の手段だったため。

```
① 10 分おきに確実に発火する        ┐
② Render の休眠サービスを起こせる   ┴→ このマシン（住宅 IP）が両方満たす
   （＋ 168 秒待てる）
```

| 項目 | 値 |
|---|---|
| 実体 | `tools\keep-render-awake.ps1` ＋ Windows タスク `komeko keep-render-awake` |
| 対象 | komeko と透明麻雀の 2 つ |
| スケジュール | 毎日 8:00 から 9 時間 55 分、10 分おき（＝ 8:00〜17:50） |
| タイムアウト | 240 秒（起動実測 158〜168 秒に対する余裕） |
| ログ | `tools\keep-render-awake.log`（直近 1000 行を保持） |
| 動作確認 | 2026-09-06 22:40 に休眠中の透明麻雀を **22.6 秒で起床**。タスク経由の実行も記録を確認済み |

```powershell
.\tools\keep-render-awake.ps1 -Register     # 登録
.\tools\keep-render-awake.ps1 -Unregister   # 解除
.\tools\keep-render-awake.ps1               # 手で 1 回だけ叩く
schtasks /Query /TN "komeko keep-render-awake" /V /FO LIST   # 状態確認
```

**弱点は「PC が起動してネットに繋がっている時しか効かない」こと。**
これを埋めるため、GitHub Actions の `schedule` を保険として残してある
（1 日 3 回しか走らないが、無いよりはよい）。

#### 未完のまま残してある選択肢: cron-job.org を「時計」にする

PC 依存を消したい場合の構成。**部品は用意済みで、トークンを入れれば動く。**

```
cron-job.org（10 分おき・正確に発火する）
     │  POST /repos/.../actions/workflows/keep-render-awake.yml/dispatches
     ↓
GitHub Actions の runner（Azure・Render を起こせる）
     ↓
komeko / 透明麻雀
```

| 項目 | 値 |
|---|---|
| ジョブ | cron-job.org の `komeko wake trigger`（**無効のまま**。ID 8394571） |
| 叩く先 | `https://api.github.com/repos/satosinakamoto-create/komeko-teppan-order/actions/workflows/keep-render-awake.yml/dispatches` |
| 方式 | POST・body `{"ref":"main"}`・成功時は 204 |
| スケジュール | `*/10 8-17 * * *`・タイムゾーン `Asia/Tokyo`（設定済み） |
| 残作業 | `Authorization` ヘッダの `Bearer PASTE_YOUR_TOKEN_HERE` を実トークンに置換し、`Enable job` を ON |
| トークン | fine-grained PAT・**対象リポジトリ 1 つ**・権限は `Actions: Read and write` **だけ** |

**cron-job.org に Render を直接叩かせてはいけない。** それが 8/21・8/27・8/28 の停止原因。
叩く相手が GitHub API なら必ず 204 が返るので、連続失敗による自動無効化も起きない。

> **トークンに有効期限を付けたら、切れる前に必ず更新すること。**
> 切れると cron-job.org が 401 を受け続け、26 回で自動無効化され、
> しかも cron-job.org の失敗通知は死んでいるので**誰も気づかない**。
> これまでの 4 回とまったく同じ壊れ方になる。

#### なぜこの形なのか

**GitHub の `schedule` は当てにならない。** 8/30〜9/5 に schedule 単独で運用した実績は
**1 日 3 回**（期待は 60 回）。公式ドキュメントに
「`schedule` イベントは高負荷時に遅延し、負荷が十分高い場合はキューに入ったジョブが
破棄されることがある」とあり、public リポジトリのスケジュールは優先度を下げられる。
15 分で眠るサービスを 4〜5 時間おきに叩いても意味がなく、
**営業時間帯 10 時間のうちカバーできていたのは約 45 分（7%）だった。**

**一方 API 経由の `workflow_dispatch` は即座に拾われる。** 優先度を下げられるのは
`schedule` だけなので、時計を外に出せば解決する。

**cron-job.org が復帰しているのは「時計」としてだけ。** 叩く相手は GitHub API なので
必ず 204 が返り、`hibernate-wake-error` も連続失敗による自動無効化も起きない。
Render を直接叩かせてはいけない（それが 8/21・8/27・8/28 の停止原因）。

#### トークンの扱い

fine-grained PAT を使い、**対象リポジトリを 1 つに絞り、権限は `Actions: Read and write` だけ**にする。
これ以上は要らない。cron-job.org の設定欄に置くことになるので、
万一漏れても「このリポジトリのワークフローを起動できるだけ」に留める。
有効期限を付けたなら、切れる前に更新すること（切れると静かに止まる）。

> **確認は GitHub の Actions タブを見る。** 10 分おきに緑が並んでいれば効いている。
> 3〜4 時間おきにしか並んでいないなら、時計（cron-job.org 側）が動いていない。

### ⚠️ 「登録した」だけでは足りない。止まっていないかを見ること（cron-job.org 時代の記録）

**2026-08-25 に発覚**: 登録はしてあったが、**2 件とも無効化されて 4 日間止まっていた**。
cron-job.org は連続で失敗したジョブを自動で無効化する。
8/21 12:10 に komeko が `output too large`、透明麻雀が `HTTP error` で失敗し、
そのまま `Enable job` が OFF に落とされていた。

その結果どうなったか。**8/25 12:30 に `/ping` を叩いたら 1 回目が 39.6 秒**（2 回目は 0.22 秒）。
昼間なのに完全に眠っていた。この状態で採用担当がリンクを踏めば、40 秒待たされる。

`/ping` 自体は正常（200・`text/plain`・本文 2 バイトの `ok`）で、
コードには何の問題も無かった。**リポジトリの外で静かに壊れていた**。

### なぜ「また止まる」のか — cron-job.org では Render を起こせない（2026-08-29 特定）

> 2026-08-27 まで、ここには「cron-job.org の 30 秒タイムアウト < Render のコールドスタート
> 39.6 秒だから必ずタイムアウトする」と書いてあった。**これは誤りだった。**
> 8/29 に応答ヘッダを捕獲して実際の原因が判明したので全面的に書き換える。

**Render は眠っているサービスを、cron-job.org からの要求では起こさない。**
即座に 503 を返す。理由はヘッダに明記されている:

```
x-render-routing: hibernate-wake-error      ← Render のルータが「起床に失敗」と言っている
Content-Length: 0
Server: cloudflare
CF-RAY: ...-CDG                             ← cron-job.org はパリ PoP から入る
```

`x-render-origin-server: Render` が**付かない**。つまりオリジンに到達しておらず、
アプリは起動を試みてすらいない。Render のアプリログに何も残らないのはこのため。

タイムアウトではない。記録した 78 回の失敗はすべて **0.23〜1.81 秒**で返っている。

#### 実地で観測した連鎖（2026-08-29 04:42〜04:47）

1 分間隔の捕獲用ジョブで、休眠中の komeko に対して記録したもの:

```
04:42:04  503 hibernate-wake-error   1.18s   完全休眠。cron-job.org の要求では起動が始まらない
04:43:03  503 hibernate-wake-error   1.34s
04:44:04  503 hibernate-wake-error   1.45s
  ├─ 04:44:17  手元から curl（15 秒で打ち切り）→ Render が起動を開始した
04:45:10  timeout                   30.01s   起動中。保留され 30 秒上限で打ち切られる
04:46:04  timeout                   30.00s   まだ起動中
  └─ 04:47:05  手元から 200 OK（x-render-origin-server: Render 付き）＝起動所要 約 163 秒
```

**cron-job.org は二重に詰んでいる。**

1. 自分の要求では起床が始まらない（`hibernate-wake-error`）
2. 誰かが別経路で起こしても、起動に約 163 秒かかるので 30 秒上限では見届けられない

だから「毎朝 8:00 に眠っている → 一日中 503 → 12:10 に自動無効化」が確定的に起きる。

#### 判定材料は送信元であって UA ではない

いずれも休眠中の komeko に対して検証済み:

| 試したこと | 結果 |
|---|---|
| 手元から cron-job.org を名乗る UA | 保留され、起動する（503 は返らない） |
| 手元から素の curl / Chrome の UA | 同上 |
| cron-job.org から（UA そのまま） | **即 503 `hibernate-wake-error`** |
| cron-job.org の TEST RUN（`X-Forwarded-For` に自分の IP が入る） | **即 503** |
| Anthropic 側のサーバから | 保留され、起動する |

cron-job.org の送信元は **`128.140.8.200`（Hetzner・ドイツ）**、
UA は `Mozilla/4.0 (compatible; cron-job.org; http://cron-job.org/abuse/)`。
UA を真似ても再現しないので、**IP／経路で扱いが変わっている**と考えるほかない。

#### 自動無効化は 26 回連続失敗で起きる

8:00 から 10 分間隔なので、**毎回きっかり 12:10 に無効化される**。
2026-08-21・08-27・08-28 の 3 回とも 12:10。偶然ではなく仕様。

#### 過去の記録との整合

- 2026-08-21 の `output too large` / `HTTP error` は履歴の保持期間切れで未確認のまま。
  ただし同じ 12:10 無効化なので、同一現象とみて矛盾しない。
- 2026-08-26 の「cron の叩き方では起きず、手で叩くと起きる」という観測は正しかった。
  当時は原因未特定だったが、`hibernate-wake-error` がその正体。

### 採った手と、採らなかった手

原因が「起動が遅いこと」ではなく「**cron-job.org からは起床が始まらないこと**」なので、
起動を速くしても解決しない。`hibernate-wake-error` は起動時間と無関係に即座に返る。

| 案 | 判定 | 備考 |
|---|---|---|
| **叩き役を別の送信元に変える（GitHub Actions）** | ✅ **採用**（2026-08-30） | 実測で起床成功。`--max-time` も自由。**無料** |
| Render 有料化（月 $7・常時起動） | 保留 | 眠らなくなるので叩き役ごと不要になる。無料で解決したので当面は不要 |
| `spring.main.lazy-initialization=true` で起動短縮 | ✗ 見送り | **起床自体が始まらないので単独では無意味だった。** 原因を取り違えていた頃の案 |
| 叩くのをやめる | ✗ | 訪問者が毎回 158〜168 秒待つ。採用担当向けには成立しない |

#### GitHub Actions で起こせることの実測（2026-08-29）

検証用の使い捨てワークフローを別ブランチに置いて手動実行した結果:

```
runner の送信元 : 20.161.78.74  AS8075 Microsoft（Virginia, US）
Cloudflare PoP  : IAD
結果            : http=200  time=167.608353s
                  x-render-origin-server: Render 付き ＝ オリジンに到達
```

`hibernate-wake-error` は出なかった。cron-job.org（Hetzner・CDG 経由）とは扱いが違う。
**Azure も Hetzner も同じデータセンター IP なので、「データセンターだから弾かれる」ではない。**
その IP レンジがどう評価されているかで分かれている、というのが最も整合する読み。
ただし Render 側の判定根拠は外からは確認できていない。

#### lazy-initialization について（もう危険ではないが、効きもしない）

`spring.main.lazy-initialization=true` はグローバル lazy 化で
`ProductionSafetyGuard` の Bean が生成されなくなり、本番の安全装置が丸ごと死ぬ。
`@Lazy(false)` で塞いであり、**この修正は 2026-08-26 の `6bee7fd` で本番に乗っている**
（`git show origin/main:src/main/java/jp/komeko/order/config/ProductionSafetyGuard.java`
に `@Lazy(false)` があることを確認済み）。

つまり安全にはなった。**が、入れる理由がもう無い。**
今回の停止の原因は起動時間ではなかったので、起動を速くしても再発は防げない。

### 当面の運用

#### 検知は GitHub に任せる（cron-job.org の通知は死んでいた）

いまは**ワークフローが失敗すれば GitHub が所有者にメールを送る**ので、これが検知役になる。
加えて失敗時は応答ヘッダを自動で記録するようにしてあるので、
「原因不明のまま終わる」ことが構造的に起きにくい。

> **cron-job.org の失敗通知は、設定が正しくても 1 通も届かなかった。**
> `execution of the cronjob fails`（3 回連続）も
> `the cronjob will be disabled because of too many failures` も ON、
> 宛先 `0709kuu@gmail.com` も正しく、アカウント側に通知を止める設定も無い。
> それでも 2026-08-27 / 08-28 の停止（各 26 回失敗＋自動無効化）で通知ゼロ。
> 8/29 に検証用ジョブ（必ず 404 する URL・1 分間隔・3 回で通知）を作っても、
> 6 回連続失敗して通知ゼロだった。原因は未特定のまま。

**この失敗から得た教訓を、設計として残しておく。**

cron-job.org は「起こす役」と「異常を知らせる役」を兼ねていた。
ところが壊れたのはまさに「起こす」部分で、しかも連続失敗するとジョブを自己停止するため、
**失敗しなくなる → 通知も鳴らない → 沈黙が正常と見分けられない**という配線になっていた。
8/21 の 4 日間も、8/27・8/28 も、これで気づけなかった。

GitHub Actions はジョブを自己停止しないので、この穴が無い。
**見張り役は、見張る対象と別の場所に置くこと。**

#### 手で確認する

```bash
curl -s -o /dev/null -w "http=%{http_code}  time=%{time_total}s\n" \
  https://komeko-teppan-order.onrender.com/ping
```

**`%{http_code}` を必ず入れる。** 旧版は所要時間しか出しておらず、
「遅い成功」と「そもそも応答なし（`http=000`）」が見分けられなかった。

判定:

| 結果 | 意味 |
|---|---|
| `http=200` / 1 秒未満 | 起きている |
| `http=200` / 数十秒〜168 秒 | 眠っていた。叩き役が効いていない |
| `http=000`（打ち切り） | 起動中。`--max-time 240` で待てば 200 になる |

**さっき自分で叩いた直後に測っても意味が無い**（自分で起こしてしまうため）。
確認は「しばらく触っていない状態」で行うこと。

叩き役が生きているかは、GitHub の実行履歴を見るのが確実:
<https://github.com/satosinakamoto-create/komeko-teppan-order/actions/workflows/keep-render-awake.yml>

緑が 10 分おきに並んでいれば効いている。**赤が出たらメールも届いているはず**で、
そのログには失敗時の応答ヘッダが記録されている。
`x-render-routing: hibernate-wake-error` が出ていたら、
GitHub Actions の送信元まで弾かれるようになったということ。
そのときは Render 有料化を検討する（月 $7・常時起動で、叩き役ごと不要になる）。

---

## 5. 公開前のチェック

- [ ] `APP_BASE_URL` が公開 URL（`https://`）になっている
- [ ] QR ページの URL が公開 URL になっている（見れば分かる）
- [ ] ログイン画面に「ゲストで参加する」が出ている
- [ ] ゲストで `/admin/settings` `/admin/staff` が **403** になる
- [ ] **管理者パスワードを変更した**（初期値のまま公開しない）
- [ ] リポジトリを public にするなら、**コミット履歴に鍵が無いか棚卸しした**

---

## 6. 訪問者がたどる道

```
ポートフォリオの GIF を見る
   ↓
「実際に触ってみる」を押す
   ↓
ログイン画面 →「ゲストで参加する」
   ↓
厨房ボード（すでに注文が並んでいる）
   ↓
サイドバー →「QRコード」
   ↓
「この卓のお客さま画面を開く」（別タブ）／ スマホで QR を読む
   ↓
注文する → 元のタブの厨房ボードに、その場で届く
```

タブを 2 つ並べれば、1 台の PC でも連携が体感できる。
スマホで QR を読んでもらえれば、実店舗とまったく同じ体験になる。
