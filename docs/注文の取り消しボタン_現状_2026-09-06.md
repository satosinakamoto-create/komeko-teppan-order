# 注文の取り消しボタン（暗06）— いまの姿と、決めること

Figma で design するための書き出し（2026-09-06）。

---

## どこにあるか・どう編集するか

| | 場所 |
|---|---|
| 画面に置いてあるもの | 「02 スマホ（お客さん）」→ **暗06 注文内容の確認**（`109:3726`）の下部ボタン帯 |
| そのボタン | `364:2042`（`暗/ボタン` のインスタンス） |
| **編集する場所** | 「03 部品」→ `暗/ボタン`（`197:4931`）の **`種類=取り消し`**（`380:2187`） |

### 直接いじれなかった理由（2026-09-06 に直した）

置いてあるのは**インスタンス**なので、暗06 の上ではラベルと種類しか変えられません。
色・高さ・角丸・枠線は、もとのコンポーネントを直す必要があります。

ところが最初は `種類=枠線` を指していました。**これを直すと
「やめる」（暗21・暗22）など他の枠線ボタンも全部変わります。**

そこで `種類=取り消し` という変種を足し、暗06 のボタンをそちらへ向けました。
**この変種は取り消しボタン専用なので、好きに変えて構いません。**
他のボタンには影響しません。

> 変種を切り替えるとラベルの上書きが外れることがあります
> （実際 1 回外れて「お会計をお願いする」に戻りました）。
> 種類を変えたら、文字が「注文を取り消す」のままか必ず見てください。

---

## いまの寸法と色

`種類=枠線` の複製なので、見た目はまだ「やめる」ボタンと同じです。

| 項目 | 値 |
|---|---|
| 文言 | 注文を取り消す |
| 大きさ | 358 × 56（画面に置くと 338 × 56） |
| 角丸 | 8 |
| 塗り | なし（地が透ける） |
| 枠線 | 1px `#333333` |
| 文字 | 16px Bold `#9E9E9E` |
| 位置 | 「この内容で注文する」の下・間隔 12px |

比較のため、同じ帯にある主要ボタン:

| | 塗り | 枠線 | 文字 |
|---|---|---|---|
| 種類=主要 | `#DD9A4E` | なし | `#141414` |
| 種類=取り消し | なし | `#333333` | `#9E9E9E` |

---

## 決めてほしいこと

### 1. この「取り消す」は何を取り消すのか ★ 未回答

**暗06 は「まだ注文は未確定です。」の画面**です。まだ厨房に送っていません。
だからここでの取り消しは、素直に読むと**送る前のカートを空にする**意味になります。

実装にはすでに 2 つの取り消しがあります。**どちらの話なのかで作るものが変わります。**

| どこ | 何ができるか | いまの見た目 |
|---|---|---|
| 注文リスト（`/cart`） | カートを空にする | **「すべて取り消す」という文字リンク**（ボタンではない） |
| 伝票・会計（`/bill`） | 送った注文を 1 件ずつ取り消す | 「ご注文 #101 をキャンセル」ボタン。**まだ焼き始めていないものだけ** |

- **前者なら**：この設計をそのまま実装します（文字リンク → ボタンに変わる）。
- **後者なら**：置き場所が違います。暗06 ではなく伝票の画面（暗04）に要ります。
  そちらのデザインが必要です。

### 2. 押し間違いをどう防ぐか

主要ボタンとの間隔は 12px です。0 だと 2 つが接して 1 つの塊に見え、
**送るつもりで取り消す**事故が起きるので離してあります。

それでも、指を伸ばした先が 12px 違うだけで結果が正反対です。決めることは 2 つ:

- **確認を挟むか。** いまの `/cart` の「すべて取り消す」は
  `confirm()` を出しています（ブラウザの素のダイアログ）。
  設計に合わせるなら、この店の見た目のモーダルを作ることになります。
- **見た目でどこまで区別するか。** いまは「やめる」と同じ枠線です。
  取り消しは戻せない操作なので、赤系にする手もあります。
  ただし**この画面に赤はまだ 1 つも無く**、地／面／きつね色の 3 色で回っています。
  色を増やすなら、他の「戻せない操作」（会計の取り消しなど）も
  同じ色にするかどうかまで含めて決めてください。

### 3. 1 品ずつ消せなくてよいか

いまの設計は「全部まとめて取り消す」1 つだけです。
行ごとの「変更」からは数量を 0 にできるので、1 品だけ消す道はあります。
ただし**「変更」を開かないと気づけません**。
行に × を置くかどうかは、この画面の混み具合との相談になります。

---

## 実装はまだしていません

このボタンは Figma にあるだけで、コードには入っていません。
上の 1 が決まってから着手します。逆向きに作ると、
置き場所ごと作り直しになります。
